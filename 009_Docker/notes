docker run -d nginx
     docker ps
    docker stop $(docker ps -a -q)
   docker ps
    docker ps -a
    docker ps --help
   docker rm $(docker ps -a -q)
   docker ps -a
   docker run -d --memory-reservation=750m nginx
 systemctl stop docker
   76  systemctl status docker
   77  systemctl disable docker
   78  systemctl status docker
   79  systemctl start docker

